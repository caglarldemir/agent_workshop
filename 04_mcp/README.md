# MCP checkpoint

Run the MCP Inspector:

```bash
mcp dev 04_mcp/server.py
```

The server exposes:
- `calculate`
- `search_web`
- `save_report`

The key idea:

**ordinary Python functions → standardized MCP tools**
